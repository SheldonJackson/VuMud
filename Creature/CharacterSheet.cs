using System;
using System.Xml;
using System.Xml.Serialization;
using Microsoft.SqlServer.Server;

namespace Creatures {
    public class CharacterSheet {

        public string FormatCharacterSheet(Creature creature)
        {
            return "";

            //string formattedCharacterSheet = string.Format("{0}'s Character Sheet{1}", creature.Name,
            //    Environment.NewLine);
            //formattedCharacterSheet += "----------------------------------";
            //formattedCharacterSheet += string.Format("Description: {0}{1}");


            //return formattedCharacterSheet;
        }

    }
}
